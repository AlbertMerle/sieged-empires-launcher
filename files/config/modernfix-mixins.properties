# This is the configuration file for ModernFix.
# In general, prefer using the config screen to editing this file.
# Changes require restarting the game to take effect.
#
# Sieged Empires client overrides:
# - Keep default optimizations.
# - Do NOT enable mixin.perf.dynamic_resources (EMF/ETF/GeckoLib/Physics Mod model risk).
# - clear_mixin_classinfo frees mixin metadata after load (RAM).

mixin.perf.dynamic_resources=false
mixin.perf.clear_mixin_classinfo=true
